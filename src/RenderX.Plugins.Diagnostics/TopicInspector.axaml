<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="RenderX.Plugins.Diagnostics.TopicInspector"
             Background="White">
    <Grid RowDefinitions="Auto,*">
        <!-- Header -->
        <Border Grid.Row="0"
                Background="#F5F5F5"
                BorderThickness="0,0,0,1"
                BorderBrush="#E0E0E0"
                Padding="12">
            <StackPanel Spacing="8">
                <TextBlock Text="Topic Inspector"
                           FontSize="12"
                           FontWeight="Bold"
                           Foreground="#333" />
                <TextBox x:Name="TopicFilterBox"
                         Watermark="Filter topics..."
                         FontSize="10"
                         Padding="6,4"
                         BorderThickness="1"
                         BorderBrush="#D0D0D0" />
            </StackPanel>
        </Border>

        <!-- Topics List -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="TopicsItemsControl"
                          Padding="12"
                          Background="White">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9F9F9"
                                BorderThickness="1"
                                BorderBrush="#E0E0E0"
                                CornerRadius="4"
                                Padding="8"
                                Margin="0,4">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="11"
                                           FontWeight="Bold"
                                           Foreground="#333" />
                                <TextBlock Text="{Binding SubscriberCount}"
                                           FontSize="10"
                                           Foreground="#666" />
                                <TextBlock Text="{Binding LastPublished}"
                                           FontSize="9"
                                           Foreground="#999" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>

