<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="RenderX.Plugins.Diagnostics.ConfigurationInspector"
             Background="White">
    <Grid RowDefinitions="Auto,*">
        <!-- Header -->
        <Border Grid.Row="0"
                Background="#F5F5F5"
                BorderThickness="0,0,0,1"
                BorderBrush="#E0E0E0"
                Padding="12">
            <StackPanel Spacing="8">
                <TextBlock Text="Configuration Inspector"
                           FontSize="12"
                           FontWeight="Bold"
                           Foreground="#333" />
                <TextBox x:Name="ConfigFilterBox"
                         Watermark="Filter configuration..."
                         FontSize="10"
                         Padding="6,4"
                         BorderThickness="1"
                         BorderBrush="#D0D0D0" />
            </StackPanel>
        </Border>

        <!-- Configuration Items -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="ConfigItemsControl"
                          Padding="12"
                          Background="White">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9F9F9"
                                BorderThickness="1"
                                BorderBrush="#E0E0E0"
                                CornerRadius="4"
                                Padding="8"
                                Margin="0,4">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{Binding Key}"
                                           FontSize="11"
                                           FontWeight="Bold"
                                           Foreground="#333" />
                                <TextBlock Text="{Binding Value}"
                                           FontSize="10"
                                           Foreground="#666"
                                           TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Type}"
                                           FontSize="9"
                                           Foreground="#999" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>

