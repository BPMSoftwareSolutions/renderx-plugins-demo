<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:RenderX.Plugins.ControlPanel"
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="600"
             x:Class="RenderX.Plugins.ControlPanel.ControlPanelControl"
             x:CompileBindings="False"
             Background="{DynamicResource Color.Background.Primary}"
             BorderThickness="1"
             BorderBrush="{DynamicResource Color.Border.Primary}">

    <UserControl.Styles>
        <!-- Property Item Hover -->
        <Style Selector="Border.property-item">
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="Background" Duration="0:0:0.2"/>
                    <BrushTransition Property="BorderBrush" Duration="0:0:0.2"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Border.property-item:pointerover">
            <Setter Property="Background" Value="{DynamicResource Color.Background.Hover}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Color.Accent.Primary}"/>
        </Style>

        <!-- Button Hover -->
        <Style Selector="Button.control-button">
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="Background" Duration="0:0:0.2"/>
                    <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.2"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Button.control-button:pointerover">
            <Setter Property="RenderTransform">
                <TransformGroup>
                    <TranslateTransform Y="-1"/>
                </TransformGroup>
            </Setter>
        </Style>

        <!-- TextBox Hover -->
        <Style Selector="TextBox.control-input">
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="BorderBrush" Duration="0:0:0.2"/>
                    <BrushTransition Property="Background" Duration="0:0:0.2"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="TextBox.control-input:pointerover">
            <Setter Property="BorderBrush" Value="{DynamicResource Color.Accent.Primary}"/>
            <Setter Property="Background" Value="{DynamicResource Color.Background.Hover}"/>
        </Style>

        <!-- Section Border Hover -->
        <Style Selector="Border.control-section">
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="Background" Duration="0:0:0.2"/>
                    <BrushTransition Property="BorderBrush" Duration="0:0:0.2"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Border.control-section:pointerover">
            <Setter Property="BorderBrush" Value="{DynamicResource Color.Accent.Primary}"/>
        </Style>
    </UserControl.Styles>

    <Grid RowDefinitions="Auto,*">
        <!-- Control Panel Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource Color.Background.Tertiary}"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource Color.Border.Primary}"
                Padding="12">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Properties"
                           FontSize="14"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
                <TextBlock Name="SelectedComponentText"
                           Text="(none selected)"
                           FontSize="12"
                           Foreground="Gray"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Control Panel Content -->
        <Border Grid.Row="1"
                Background="{DynamicResource Color.Background.Primary}"
                Padding="12">
            <ScrollViewer>
                <StackPanel Spacing="12">
                    <!-- Properties Section -->
                    <Border Classes="control-section"
                            Background="{DynamicResource Color.Background.Secondary}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource Color.Border.Primary}"
                            CornerRadius="4"
                            Padding="12">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Component Properties"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource Color.Border.Primary}"/>

                            <!-- Property Grid -->
                            <ItemsControl Name="PropertiesItemsControl"
                                          Background="{DynamicResource Color.Background.Primary}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Classes="property-item"
                                                Background="{DynamicResource Color.Background.Primary}"
                                                BorderThickness="0,0,0,1"
                                                BorderBrush="{DynamicResource Color.Border.Primary}"
                                                Padding="0,8">
                                            <Grid ColumnDefinitions="120,*">
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Key}"
                                                           FontSize="11"
                                                           VerticalAlignment="Center"
                                                           Foreground="#666"/>
                                                <TextBox Classes="control-input"
                                                         Grid.Column="1"
                                                         Text="{Binding Value}"
                                                         FontSize="11"
                                                         Margin="8,0,0,0"
                                                         Padding="6,4"
                                                         BorderThickness="1"
                                                         BorderBrush="{DynamicResource Color.Border.Secondary}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Interactions Section -->
                    <Border Classes="control-section"
                            Background="{DynamicResource Color.Background.Secondary}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource Color.Border.Primary}"
                            CornerRadius="4"
                            Padding="12">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Interactions"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource Color.Border.Primary}"/>

                            <!-- Interaction Buttons -->
                            <ItemsControl Name="InteractionsItemsControl"
                                          Background="{DynamicResource Color.Background.Primary}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Classes="control-button"
                                                Content="{Binding Name}"
                                                Click="OnInteractionButtonClicked"
                                                Padding="8,6"
                                                Margin="0,4"
                                                Background="#2196F3"
                                                Foreground="{DynamicResource Color.Background.Primary}"
                                                FontSize="11"
                                                CornerRadius="4"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- CSS Classes Section -->
                    <Border Classes="control-section"
                            Background="{DynamicResource Color.Background.Secondary}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource Color.Border.Primary}"
                            CornerRadius="4"
                            Padding="12">
                        <StackPanel Spacing="8">
                            <TextBlock Text="CSS Classes"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource Color.Border.Primary}"/>

                            <!-- CSS Classes Input -->
                            <TextBox Classes="control-input"
                                     Name="CssClassesInput"
                                     Watermark="Enter CSS classes..."
                                     FontSize="11"
                                     Padding="6,4"
                                     BorderThickness="1"
                                     BorderBrush="{DynamicResource Color.Border.Secondary}"
                                     AcceptsReturn="False"/>

                            <Button Classes="control-button"
                                    Content="Apply CSS Classes"
                                    Click="OnApplyCssClassesClicked"
                                    Padding="8,6"
                                    Background="{DynamicResource Color.Success}"
                                    Foreground="{DynamicResource Color.Background.Primary}"
                                    FontSize="11"
                                    CornerRadius="4"/>
                        </StackPanel>
                    </Border>

                    <!-- Status Section -->
                    <Border Classes="control-section"
                            Background="{DynamicResource Color.Background.Secondary}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource Color.Border.Primary}"
                            CornerRadius="4"
                            Padding="12">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Status"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource Color.Border.Primary}"/>
                            <TextBlock Name="StatusText"
                                       Text="No component selected"
                                       FontSize="11"
                                       Foreground="Gray"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>

