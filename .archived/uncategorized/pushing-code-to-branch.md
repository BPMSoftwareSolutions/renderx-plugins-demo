---
type: "manual"
---

when pushing code to the repo, set a timer for 2 minutes (120 seconds) via the CLI and check to wait for the remote CLI to run
when the timer is done, check the remote CLI to ensure it passes. If it doesn't, fix the error and push the fix until green.
