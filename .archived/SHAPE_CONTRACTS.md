# Shape Contracts (Placeholder)

Contracts define stable set of required telemetry fields per feature.

Baseline Required Fields:
- feature, event, beats, status, correlationId, shapeHash (hashing-enabled features)
- coverageId (coverage-coupling features)

Change Process:
1. Proposed contract update
2. Review + annotation (future approval registry)
3. CI enforcement
