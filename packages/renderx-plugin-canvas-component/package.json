{
  "name": "@renderx-plugins/canvas-component",
  "version": "0.0.0-private",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts src/symphonies/**/*.ts --format esm --splitting --out-dir dist --clean",
    "test": "vitest --run --config vitest.config.ts"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts"
    },
    "./symphonies/*": {
      "import": "./src/symphonies/*"
    }
  }
}
